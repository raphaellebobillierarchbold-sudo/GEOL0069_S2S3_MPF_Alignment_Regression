# Processed data

This folder contains small processed datasets generated by `notebooks/02_create_aligned_misaligned_datasets.ipynb`.

Expected files:

- `training_data_noalign.npz`
- `training_data_aligned.npz`
- `test_data.npz`

These are derived products used by the regression notebooks. They can be regenerated from the raw Week 6 `.npz` files.
